@model IEnumerable<RefTemeto.Models.Settlement>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-8">
            <h2 class="text-primary">Magyarországon található települések irányítószámai</h2>
        </div>
        <div class="col-4 text-right">
            <a asp-controller="Settlement" asp-action="Create" class="btn btn-primary">
                Új település felvétele
            </a>
        </div>
    </div>
    <br /> <br />

    @if (Model.Count() > 0)
    {
        <table class="table-bordered table-striped" style="width: 70%">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Irányítószám
                    </th>
                    <th>
                        Település
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                <tr>
                    <td width="20%"> @obj.SettlementId      </td>
                    <td width="20%"> @obj.PostalCode        </td>
                    <td width="30%"> @obj.Station           </td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Settlement" asp-route-Id="@obj.SettlementId" asp-action="Edit" class="btn btn-primary mx-2">
                                Szerkeszt
                            </a>
                            <a asp-controller="Settlement" asp-route-Id="@obj.SettlementId" asp-action="Delete" class="btn btn-danger mx-2">
                                Töröl
                            </a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>Nincsenek megjeleníthető adatok.</p>
    }
</div>